<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.*?>

<HBox styleClass="top-nav" spacing="12" alignment="CENTER_LEFT" 
      xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.noteflix.pcm.ui.components.NavbarController">
    
    <padding>
        <Insets top="12" right="20" bottom="12" left="20"/>
    </padding>
    
    <!-- Brand -->
    <HBox spacing="10" alignment="CENTER_LEFT">
        <ImageView fitHeight="24" fitWidth="24" preserveRatio="true">
            <image>
                <Image url="@../../images/icons/app-icon.png"/>
            </image>
        </ImageView>
        <Label text="PCM Desktop" styleClass="title-3"/>
    </HBox>
    
    <Region HBox.hgrow="ALWAYS"/>
    
    <!-- Right Actions -->
    <HBox spacing="8" alignment="CENTER_RIGHT">
        <!-- New Screen Button - Accent -->
        <Button text="＋ New Screen" onAction="#handleNewScreen" styleClass="accent"/>
        
        <!-- Notification Button -->
        <Button text="🔔" onAction="#handleNotifications" styleClass="flat">
            <tooltip>
                <Tooltip text="Notifications"/>
            </tooltip>
        </Button>
        
        <!-- Settings Button -->
        <Button text="⚙" fx:id="settingsBtn" onAction="#handleSettings" styleClass="flat">
            <tooltip>
                <Tooltip text="Settings"/>
            </tooltip>
        </Button>
    </HBox>
</HBox>
