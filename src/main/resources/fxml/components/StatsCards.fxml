<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<HBox styleClass="stats-grid" spacing="16" xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.noteflix.pcm.ui.components.StatsCardsController">
    
    <VBox styleClass="stat-card">
        <HBox alignment="CENTER_LEFT" spacing="8">
            <Label text="⚡" styleClass="stat-icon"/>
            <Label text="Events" styleClass="stat-title"/>
        </HBox>
        <Label fx:id="eventsValue" text="5" styleClass="stat-value"/>
        <Label fx:id="eventsChange" text="+2 this week" styleClass="stat-change positive"/>
    </VBox>
    
    <VBox styleClass="stat-card">
        <HBox alignment="CENTER_LEFT" spacing="8">
            <Label text="📄" styleClass="stat-icon"/>
            <Label text="Source Files" styleClass="stat-title"/>
        </HBox>
        <Label fx:id="filesValue" text="3" styleClass="stat-value"/>
        <Label fx:id="filesChange" text="No changes" styleClass="stat-change neutral"/>
    </VBox>
    
    <VBox styleClass="stat-card">
        <HBox alignment="CENTER_LEFT" spacing="8">
            <Label text="🗄️" styleClass="stat-icon"/>
            <Label text="DB Tables" styleClass="stat-title"/>
        </HBox>
        <Label fx:id="tablesValue" text="3" styleClass="stat-value"/>
        <Label fx:id="tablesChange" text="All synced" styleClass="stat-change positive"/>
    </VBox>
</HBox>

